<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.gdgvit.aiesec">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".utility.AppDetails"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Main.MainActivity" />
        <activity
            android:name=".activity.Main.ActivityLogin"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".activity.Main.StartActivity"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.EP.ActivityEpMain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.EP.EPSignUpActivity" />
        <activity android:name=".activity.FileSelection.FileSelectionActivity" />
        <activity android:name=".activity.Aiesecer.ASignUpActivity" />
        <activity android:name=".activity.Main.oGet.SignUpActivity"></activity>
    </application>

</manifest>